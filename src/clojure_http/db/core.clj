(ns clojure-http.db.core)


(def database {:dbtype "postgresql"
               :dbname "bookstore"
               :host "localhost"
               :port "5432"
               :user "postgres"
               :password "mysecretpassword"})

